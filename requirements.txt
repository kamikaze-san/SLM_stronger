# Install torch first with CUDA 12.4 build:
# pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124

torch==2.6.0
transformers==5.7.0
accelerate
datasets==3.3.2
peft
verl==0.7.1
numpy
pandas==2.2.3
matplotlib==3.10.0
seaborn==0.13.2
tqdm==4.67.1

# Optional: faster attention (must match torch+cuda version exactly)
# pip install flash_attn
